/*
Error 1: invalid parameter list (invalid parameter syntax)
Error 2: invalid individual parameter (invalid ID syntax)
*/

void main(void main(), int b) { /* error 1 */
  int x;
  int fac;
  x = input();
  fac = 1;
  while (x > 1) {
    fac = fac * x;
    x = x - 1;
  }

  output(fac);
}

void main2(int main2()){ /* error 2 */
  int s;
}